Header letterhead polish. File touched: styles.css only. LOGO.md untouched.
header { text-align: center } so lockup, tagline, and nav read as one stacked mark.
main is still left-aligned; shared header/main column max-width and side padding are unchanged.
.tagline margin-top is 2rem (was 0.25rem) so the plus-and-SURMOUNT lockup sits as its own object.
nav { margin-top: 1rem } so the links are not glued to the tagline.
nav li horizontal margin is 0.625rem left and right (was 1.25rem right only) so the centered row is even.
Plus construction, copy, colors, and font files are unchanged. No minify. No commit.
The lockup itself still uses the existing .logo flex centering and 0.85rem wordmark air.
Pages inherit the same header CSS: index.html, philosophy.html, support.html.
Verify by eye: emblem, then air, then tagline, then a little air, then a centered Home / Philosophy / Support row.
