The plus is two overlapping white rects on a full-bleed #000000 32x32 square: vertical x=13 y=4 w=6 h=24, horizontal x=4 y=13 w=24 h=6.
Bar thickness is 6/32 (18.75%), integer and optically centered; equal arms, no Unicode plus, no flares, no outline, no gradient, no alpha.
Only #000000 and #FFFFFF appear in the SVG files.
favicon.ico was replaced (not removed) so /favicon.ico no longer serves the old mountain-peak icon.
The ICO is a 16x16 opaque plus with the same 3/16 proportion, drawn with ImageMagick rectangles (pixels 6,2–8,13 and 2,6–13,8).
ImageMagick writes ICO as 32bpp; every pixel is #000000 or #FFFFFF with alpha 255 (no transparency).
logo.svg was added, same viewBox and two-rect geometry as favicon.svg, no wordmark and no font.
HTML, styles.css, fonts, and LOGO.md were not edited.
